# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Package versions must come only from the installed SDK's `bundledNativeModules.json` — never "from memory".
